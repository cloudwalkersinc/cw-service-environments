# Dev environment - shadow production flavor
replicaCount: 1

image:
  repository: cloudwalkersinc/paved-road-service
  pullPolicy: IfNotPresent
  tag: ''  # Matches production version (Kargo syncs from prod)

service:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP

env:
  - name: ENVIRONMENT
    value: 'dev'
  - name: FLAVOR
    value: 'devprod'
  - name: LOG_LEVEL
    value: 'warn'  # Prod-like logging
  - name: SHADOW_PROD
    value: 'true'

# Mirror production resource allocation (but scaled down)
resources:
  limits:
    cpu: 1000m
    memory: 1Gi
  requests:
    cpu: 250m
    memory: 256Mi

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 70

livenessProbe:
  httpGet:
    path: /health
    port: http
  initialDelaySeconds: 30
  periodSeconds: 10

readinessProbe:
  httpGet:
    path: /ready
    port: http
  initialDelaySeconds: 10
  periodSeconds: 5

ingress:
  enabled: true
  className: 'nginx'
  hosts:
    - host: paved-road-devprod.cloudwalkersinc.com
      paths:
        - path: /
          pathType: Prefix
